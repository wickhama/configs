(load-file "~/.emacs.d/custom-agenda.el")
(setq org-agenda-files custom-org-agenda-files)

;company mode + flyspell in org mode
(add-hook 'org-mode-hook '(lambda ()
			     (local-set-key (kbd "C-c m") 'company-math-symbols-unicode)))
